SELECT COLUMN_NAME,DATA_TYPE,LEN(COLUMN_NAME)

FROM INFORMATION_SCHEMA.COLUMNS

WHERE TABLE_NAME = 'ABC'

ORDER BY ORDINAL_POSITION

SELECT COLUMN_NAME

FROM INFORMATION_SCHEMA.COLUMNS

WHERE TABLE_NAME = 'ABC' OR TABLE_NAME = 'ABC_CSTM'

ORDER BY TABLE_NAME

EXEC sp_columns 'ABC'